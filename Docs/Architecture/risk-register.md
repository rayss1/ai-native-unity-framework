# Architecture Risk Register

Status: Active for the first vertical slice
Last updated: 2026-08-26

Scale: likelihood and impact are Low/Medium/High. “Tripwire” is the earliest objective signal that forces action. Owners are roles until modules have maintainers.

| ID | Risk | Likelihood | Impact | Owner | Tripwire / validation | Mitigation and migration | Rollback / containment |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R-01 | Fantasy fork license or ownership changes block commercial distribution | Low | High | Project owner + legal | License, copyright holder, prohibited-entity relationship, distribution model, or adopted baseline changes | Preserve notices and the recorded project-owner approval; reopen review on any tripwire | Stop new distribution; retain the last approved artifact or replace the adapter/foundation |
| R-02 | Deep Fantasy changes make upstream/security updates impractical | High | High | Server platform | Patch queue cannot rebase cleanly or upstream merge fails replay/load suite | Pin upstream; focused commits; fork changelog; scheduled sync rehearsal | Retain previous fork pin/image; replace only failing subsystem behind adapter |
| R-03 | Fantasy types leak into Shared or product modules | Medium | High | Architecture | Manifest/API validator finds forbidden reference or public signature | ADR-0002 validator; adapter contracts; review public APIs | Revert leaking API; compatibility shim with removal issue |
| R-04 | The accepted .NET 10 production baseline ages past its support window | Low | High | Server platform + release | SDK/runtime/container pins fall behind servicing or no successor review is scheduled before support expiry | Track servicing releases, audit dependencies, and start a same-matrix successor Spike before the support deadline | Restore the last qualified image; keep Shared on `netstandard2.1`; adopt a replacement through a superseding ADR |
| R-05 | Shared code compiles but diverges across Unity/server | Medium | High | Shared gameplay | Golden vector event/hash mismatch or conditional code added | Same source set; canonical hash; cross-runtime CI; explicit adapters | Revert incompatible change; never fork Shared source |
| R-06 | Unity/server physics mismatch causes visible correction or invalid hit results | High | High | Gameplay + physics | Correction/error gates fail under Regional profile | Constrain predicted model, normalize queries, tune reconciliation; evaluate client Jolt | Return to simpler prediction/Unity adapter; server remains authoritative |
| R-07 | Jolt native binding is unstable, slow, or costly on mobile | Medium | High | Physics adapter | ABI crash/leak, >3 ms P99, nonzero Tick allocations, unacceptable binary cost | Stable C ABI, sanitizer/native tests, representative benchmark, isolated lifecycle | Swap adapter or restrict Jolt to server; retain test fake and Unity client path |
| R-08 | Navigation work blocks fixed Tick or produces incompatible artifacts | Medium | High | Navigation + Tools | Tick wait detected, P99 path >25 ms, bake hash drift, version mismatch | Offline reproducible bake; bounded async jobs; versioned artifact/header | Disable affected nav feature, use prior artifact/adapter, fail safe on load |
| R-09 | Replication exceeds CPU or bandwidth at 64 players | High | High | Realtime replication | Tick/packet/bandwidth gate red at 20 Hz | AOI, delta, quantization, priority; profile before bit-packed codec | Reduce snapshot rate/AOI/detail; return to prior codec/config |
| R-10 | Slow client stalls room or grows unbounded memory | Medium | High | Realtime transport | Backpressure profile adds >=0.5 ms P99 or queue exceeds cap | Per-client bounded queues; coalescing; explicit send outcomes; disconnect policy | Drop replaceable snapshots, recover baseline, then disconnect isolated client |
| R-11 | Protocol change breaks mixed-version mobile fleet | Medium | High | Shared contracts | Compatibility/golden/fuzz test fails or unsupported capability observed | Additive policy, reserved IDs, dual decode, capability telemetry | Restore prior encoder; keep new decoder and never reuse deployed IDs |
| R-12 | Custom content update bricks startup or activates corrupt content | Medium | High | Client content | Crash-injection/integrity/offline test exposes no bootable active set | Signed manifests, verify-before-activate, two retained sets, AOT recovery | Boot last-known-good manifest; resource-only/store release path |
| R-13 | HybridCLR conflicts with AOT/stripping/store policy | Medium | High | Client release | Mobile build/recovery test fails or policy review rejects scope | Optional package; limited Gameplay/UI scope; release-specific review | Remove package/disable cohort; AOT and content rollback remain functional |
| R-14 | Telemetry cardinality or exporter outage harms runtime | Medium | Medium | Observability | Unbounded series/buffer or Tick delta >=0.25 ms P99 | Label allowlist; async bounded export; sampling; outage tests | Disable exporter/config, count drops locally; gameplay continues |
| R-15 | Runtime plugin composition becomes implicit or cyclic | Medium | High | Architecture + module owners | Runtime scan/reflection added; graph cycle; unused package still referenced | Build-time Composition Roots; manifest graph CI; optional-package fixture | Remove registration/reference; revert composition manifest |
| R-16 | Agent context drifts and agents make boundary-breaking edits | High | Medium | Agent engineering | Generated context drift, validator bypass, claimed evidence not reproducible | Repository-owned sources, freshness/architecture checks, bounded repair workflow | Disable faulty automation; revert scoped edits; documented contracts stay authoritative |
| R-17 | Premature infrastructure (Kubernetes/broker/Redis) expands failure surface | Medium | Medium | Infrastructure | Dependency proposed without measured bottleneck/availability requirement | Require ADR with workload, SLO, operational owner, exit criteria | Remove optional deployment component; return to Compose/simple topology |
| R-18 | Performance results are not reproducible or hide tail failures | Medium | High | Performance owner | Missing SHA/config/seed/percentiles or run cannot replay | Standard report metadata, stored seeds/vectors, release-equivalent builds | Reject result and retain previous accepted budget/config |
| R-19 | A mutable, unqualified, or unverifiable image is promoted or cannot be rolled back | Medium | High | Release owner | Tag already exists, qualification identity differs, attestation fails, or prior digest/config is unavailable | Manual exact-main gate; immutable source/version tags; SBOM/provenance/attestation; append-only repository release ledger; digest deployment; retained previous digest/config | Stop promotion, drain the new Host, restore the previous digest/config; never overwrite the failed version tag. The first release has no prior image, so its pre-success containment is drain-and-stop. |
| R-20 | Increasing rooms per process hides aggregate CPU, memory, scheduler-tail, or per-client wire saturation | Medium | High | Server performance | Two-room Tick, CPU, memory, connection, replay, or wire result loses 20% headroom; room routing or replay identity is ambiguous | Retain replay-enabled exact-main run `32976156874`; keep production at one room until a named environment canary with qualified candidate/fallback identities and operator rollback authority passes | Reject the density increase and return to one room per process; do not weaken budgets or down-convert/reuse ambiguous replay evidence |

## Review cadence

Review this register at each vertical-slice milestone and whenever a tripwire fires. Closing a risk requires linked evidence and either removal of the underlying condition or transfer to an operational register; “test passed once” is not sufficient for ongoing fork/runtime/policy risks.
